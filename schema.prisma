datasource db {
  provider = "sqlite"
  url      = "file:./marriage_countdown.db"
}

generator client {
  provider = "prisma-client-js"
}

model MarriageCountdown {
  id        Int      @id @default(autoincrement())
  eventDate DateTime
  notes     String?
}
